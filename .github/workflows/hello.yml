name: Hello
on: push:
         branches: main
jobs: 
     Greet:
           runs-on: ubuntu-latest
           steps: 
                 - uses: actions/checkout
                 - run: echo "hello from gituhub actions"

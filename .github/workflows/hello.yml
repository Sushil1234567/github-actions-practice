name: Hello
on: 
   push:
        branches: [main]
jobs: 
     Greet:
           runs-on: ubuntu-latest
           steps: 
                 - uses: actions/checkout@v4
                 - run: echo "hello from gituhub actions"
